import { Request, Response } from "express";

export default function extract(req: Request, res: Response) {
    try {

    } catch {

    }
}

export async function extract_articles(articleUrl: string) {

}