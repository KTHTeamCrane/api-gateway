import unit from "../src/unit";

describe("testing the unit test lol", () => {
    test("should return 4 since 2 + 2 = 4", () => {
        // test code goes here
        expect(unit(2, 2)).toBe(4)
    })
})